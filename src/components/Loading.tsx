import React from 'react'

type Props = {}

const Loading = (props: Props) => {
  return (
    <div className='w-full h-full flex items-center justify-center textgradient text-6xl font-serif'>Loading</div>
  )
}

export default Loading